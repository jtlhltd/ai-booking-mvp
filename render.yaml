services:
  - type: web
    name: ai-booking-mvp
    env: node
    # Install devDependencies so Jest runs; fails the deploy if tests fail
    buildCommand: npm ci --include=dev && npm test
    startCommand: npm run render-start
    plan: starter
    envVars:
      - key: NODE_VERSION
        value: 18
