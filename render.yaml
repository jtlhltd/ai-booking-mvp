services:
  - type: web
    name: ai-booking-mvp
    env: node
    buildCommand: npm ci
    startCommand: npm run render-start
    plan: starter
    envVars:
      - key: NODE_VERSION
        value: 18
