node_modules
.env
.env.test
.cursor/cloud-agent-secrets.env
.cursor/self-heal-secrets.env
coverage
coverage-tmp/
data/*.log
data/*.tmp
data/*.db

# Local exports / temp data (never commit)
data/_tmp_*.csv
data/_tmp_*.json
data/_tmp_*.txt

# Local databases
data/database.json

# Python cache artifacts
__pycache__/
*.pyc

# Cloud / API credential JSON (prefer env vars; never commit keys)
credentials.json
client_secret*.json
*credentials*.json
*service-account*.json
*-firebase-adminsdk*.json

# GCP / agency keys (never commit)
ai-agency-*.json

# Call logs
call-logs-*.csv
call-logs-*.json
lib/call-logs-*.csv

# Screenshots / one-off assets
ChatGPT Image*.png
public/ChatGPT*.png

# Frontend build output
public/build/

# Cursor workspace metadata (never commit)
.cursor/*
!.cursor/rules/
!.cursor/rules/*.mdc
!.cursor/self-heal-secrets.env.example

# Playwright / local test output
playwright-report/
test-results/

# One-off agent / local scratch files
tmp-*.txt

# Local DB dumps (never commit)
backups/
*.dump
database-data-only.sql

